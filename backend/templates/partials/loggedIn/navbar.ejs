  <nav  class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/user/dashboard">📚 BookNook</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item mx-2">
            <a class="nav-link active" href="/user/dashboard">Home</a>
          </li>
          <li class="nav-item mx-2">
            <a class="nav-link active" href="/profile">Profile</a>
          </li>
          <li class="nav-item mx-2">
            <a class="nav-link active" href="/books/addBook">Add Book</a>
          </li>
          <li class="nav-item mx-2">
            <a class="nav-link active" href="/books/myBooks">My Listings</a>
          </li>
          <li class="nav-item mx-2">
            <a class="nav-link text-danger" href="/user/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <script>
  let lastScroll = 0;
  const navbar = document.querySelector(".navbar");

  window.addEventListener("scroll", () => {
    const currentScroll = window.pageYOffset;
    if (currentScroll <= 0) {
      navbar.classList.remove("scroll-up");
      return;
    }

    if (currentScroll > lastScroll && !navbar.classList.contains("scroll-down")) {
      navbar.classList.remove("scroll-up");
      navbar.classList.add("scroll-down");
    } else if (currentScroll < lastScroll && navbar.classList.contains("scroll-down")) {
      navbar.classList.remove("scroll-down");
      navbar.classList.add("scroll-up");
    }
    lastScroll = currentScroll;
  });
</script>

<style>
  .navbar.scroll-down {
    transform: translateY(-100%);
    transition: transform 0.3s ease-in-out;
  }
  .navbar.scroll-up {
    transform: translateY(0);
    transition: transform 0.3s ease-in-out;
  }
</style>
