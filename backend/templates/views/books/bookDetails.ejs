<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- book.title %> | BookNook</title>
  <%- include('../../partials/header')%>
</head>
<body class="bg-light">

  <!-- Navbar -->
  <%- include('../../partials/loggedIn/navbar')%>

  <!-- Book Details Section -->
  <div class="container my-5">
    <div class="card shadow-sm p-4">
      <div class="row g-4">
        <!-- Book Image -->
         <a href="/user/dashboard">
           <i class="bi bi-arrow-left"></i>
         </a>
        <div class="col-md-5 text-center">
          <img src="<%- book.image %>" alt="<%= book.title %>" class="img-fluid rounded" style="max-height: 400px; object-fit: cover;">
        </div>

        <!-- Book Info -->
        <div class="col-md-7">
          <h2 class="fw-bold"><%- book.title %></h2>
          <p class="text-muted mb-2"><strong>Author:</strong> <%- book.author %></p>
          <p class="text-muted mb-2"><strong>Category:</strong> <%- book.category %></p>
          <p class="text-muted mb-2"><strong>Condition:</strong> <%- book.condition %></p>
          <h4 class="text-success mt-3 mb-3">₹ <%- book.price %></h4>
          <p><strong>Description:</strong></p>
          <p class="text-muted"><%- book.description %></p>

          <hr>

          <!-- Seller Info -->
          <h5 class="fw-bold mb-3">Seller Information</h5>
          <p class="mb-1"><strong>Name:</strong> <%- user.firstname %> <%- user.lastname %></p>
          <p class="mb-1"><strong>Email:</strong> <%- user.email %></p>
          <p class="mb-1"><strong>Phone:</strong> <%- user.phone %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <%- include('../../partials/footer')%>
</body>
</html>
