<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Books | BookNook</title>
  <%- include('../../partials/header')%>
</head>
<body class="bg-light">

  <!-- Navbar -->
    <%- include('../../partials/loggedIn/navbar')%>

  <!-- Page Heading -->
  <div class="container mt-5">
    <div class="text-center mb-4">
      <h2 class="fw-bold">My Listed Books</h2>
      <p class="text-muted">Here are all the books you’ve added for sale.</p>
    </div>

    <!-- Books Grid -->
    <div class="row">
      <% if (books && books.length > 0) { %>
        <% books.forEach(book => { %>
          <div class="col-md-4 mb-4">
            <div class="card shadow-sm h-100">
              <img src="<%= book.image %>" class="card-img-top" alt="<%= book.title %>" style="height: 230px; object-fit: cover;">
              <div class="card-body d-flex flex-column">
                <div class="d-flex justify-content-between">
                  <h5 class="card-title flex-column"><%= book.title %></h5>
                  
                      <div class="form-check form-switch me-3">
                        <input class="form-check-input" type="checkbox" role="switch" id="switchCheck_<%= book._id %>"
                        <%= book.isPublic ? "checked" : "" %> onchange="changeInput('<%= book._id %>', this.checked)">
                      <label class="form-check-label" for="switchCheck_<%= book._id %>"><%= book.isPublic ? "Public" : "Private" %></label>

                    </div>
                  
                </div>
                <p class="card-text text-muted small mb-1"><strong>Author:</strong> <%= book.author %></p>
                <p class="card-text text-muted small"><strong>Condition:</strong> <%= book.condition %></p>
                <p class="text-success fw-semibold mt-2">₹ <%= book.price %></p>
                <p class="card-text text-truncate"><%= book.description %></p>
                <div class="mt-auto d-flex justify-content-between">
                  <a href="/books/details/<%= book._id %>" class="btn btn-outline-primary btn-sm">View</a>
                  <a href="/books/edit/<%= book._id %>" class="btn btn-outline-warning btn-sm">Edit</a>
                  <form action="/books/delete/<%= book._id %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-outline-danger btn-sm" onclick="return confirm('Delete this book?')">Delete</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="text-center mt-5">
          <h5 class="text-muted">You haven’t added any books yet.</h5>
          <a href="/books/addBook" class="btn btn-primary mt-3">+ Add Your First Book</a>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Footer -->
  <%- include('../../partials/footer')%>
<!-- <script>
  function changeInput(e){
    // console.log("Public")
    // console.log(e)
    let a = document.getElementById('switchCheckChecked').checked
    console.log(a)
    // window.location.href="/books/myBooks/public";
  }
</script> -->
<script>
  function changeInput(bookId, isChecked) {
    fetch(`/books/myBooks/${bookId}`, {
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ isPublic: isChecked })
    })
    .then(res => res.json())
    .then(data => {
      // Optional: Show a toast or feedback
      // console.log('Status updated: ', data);
      // alert("Updated Successfully");
      location.reload();
    })
    .catch(err => {
      alert('Something went wrong');
      console.error(err);
    });
  }
</script>

</body>
</html>
